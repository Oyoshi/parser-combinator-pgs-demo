import styled from "styled-components";

const SCHeader = styled.header`
  text-align: center;
  background: yellow;
`;

const App = () => (
  <SCHeader>
    <h1>Hello World</h1>
  </SCHeader>
);

export default App;
